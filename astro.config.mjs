import { defineConfig } from 'astro/config';

// Set `site` til den endelege adressa når du veit ho (trengst for absolutte lenker/sitemap).
export default defineConfig({
  // site: 'https://dagensdobbel.no',
});
